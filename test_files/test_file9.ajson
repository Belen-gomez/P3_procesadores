//Errores con ajson
//No puede haber un ajson vacio
type ajson = { };
let circle: Circle ={};

type NestedObject = { "this is": 3 * 3, prop2: int, "prop3": boolean };

ajson ={
    "this is": "a json"
};

//Errores en las claves de un ajson
let circle: Circle ={
    10: 10,
    tr: 10,
    +: 10
};

//Errores en los valores de un ajson
let circle: Circle ={
    x: +,
    y: 10,
    radio: 10
};

//Errores de estructura
let circle: Circle ={
    x: +
    y: 10
    radio: 10
};

let circle: Circle ={
    x +,
    y 10,
    radio: 10
};

let circle: Circle ={
    x +,
    y 10,
    radio: 10

